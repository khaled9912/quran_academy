# Features Architecture

This folder contains domain-specific features organized with their own API, components, hooks, types, validations, and pages.

Each feature should follow this pattern:

```
features/{feature}/
├── api/
├── components/
├── hooks/
├── types/
├── validations/
└── pages/
```

Use this layout to keep feature logic encapsulated and make the app easier to scale.
